# 修改说明 2026-09-12 —— H3 锁人物：角色外观描述绑定

## 背景
H3（U01/U01H）锁人物需要提示词中「文字外观描述 + <Picture N> 图片引用」双重锚定。
此前软件端只传 subject_names（角色名），提示词里只有 "is the character shown in <Picture N>"，
缺少外观文字，模型锚定弱，人物漂移。

## 改动
文件：src/components/NewScript/VideoGenBoard.jsx（win 与 macOS 已同步）
1. import { extractAppearance } from "./CharacterManager.jsx"
2. 单分镜视频任务（i2v/s2v 高级生成分支）：
   - 构造 subjectDescs = 分镜绑定角色逐个 extractAppearance()（与 ref_image 人物图顺序一一对应）
   - workflowParams.subject_descs = subjectDescs（随任务提交给调度机）
   - 日志："角色外观描述绑定：N个角色，首角色描述：…"
3. 调度机 wan_self_worker.py 已支持 subject_descs：
   - 有描述 -> <Subject N> is the character shown in <Picture N>, {外观描述}. Keep exactly as in both...
   - 无描述 -> 回退原写法
4. 无首帧(s2v)与有首帧(i2v)两条链路都走该分支；ia2v(托管)/t2v 未改。

## 验证
- npm run build 通过（vite build 12.86s）
- 调度机容器已重建生效
- 明天实测：软件端提交任务后，调度机日志可见完整提示词与 descs
